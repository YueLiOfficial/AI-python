### 分类模型评估指标

混淆矩阵

准确率：预测正确的样本数与所有样本数的比例

精确率（查准率）：预测为正例的样本数占所有实际为正例的样本数的比例

召回率（查全率）：实际为正例的样本中预测为正例的比例

F1分数：精确率和召回率的调和平均

评估报告：classification_report

### 线性回归

线性回归是一种用于建立两个或多个变量之间线性关系的统计学习方法，通过拟合一条直线或一个超平面，描述输入特征与目标变量之间的线性关系。

在线性回归中，常用均方误差（MSE）作为损失函数，用于衡量模型预测值与真实值之间的差异。

线性回归的参数可以通过解析方法求解，也可以使用梯度下降法进行迭代优化。梯度下降法是一种迭代优化算法，它首先给参数设置一个初始值，然后计算损失函数关于参数的梯度，并沿着梯度的负方向不断更新参数，使损失函数逐渐减小，最终找到使损失函数达到最小值的参数。